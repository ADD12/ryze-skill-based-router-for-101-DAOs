version: '3.8'

services:
  rye-api:
    build: .
    container_name: rye-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/rye.db
    restart: unless-stopped